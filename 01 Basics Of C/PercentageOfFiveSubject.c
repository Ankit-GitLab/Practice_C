#include <stdio.h>
int main()
{
    float Math = 72;      // marks
    float Physics = 73;   // marks
    float Chemistry = 59; // marks
    float English = 66;   // marka
    float Hindi = 69;     // marks
    float Precentage = (Math + Physics + Chemistry + English + Hindi) / 5;
    printf("Percentage of five subject =%f", Precentage);
    return 0;
}