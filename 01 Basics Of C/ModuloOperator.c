#include <stdio.h>
int main()
{
    int A = 8; // a>b always
    int B = 6;
    int Remainder = A % B; // r = remainder
    printf("%d", Remainder);
    return 0;
    
} // jab hum log  a<b = late ha to answer humsa zero aata hai