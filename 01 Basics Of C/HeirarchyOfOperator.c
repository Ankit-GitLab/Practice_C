#include <stdio.h>
int main()
{
    int A = 2 * 3 / 4 + 4 / 4 + 8 - 2 + 5.0 / 8; // int = humsa integer store karta hai
    printf("%d", A);
    float B = 2 * 3 / 4 + 4 / 4 + 8 - 2 + 5.0 / 8; // float = humsa real or decimal number store karta hai
    printf("\n%f", B);
    return 0;
}