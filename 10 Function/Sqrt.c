#include<stdio.h>
#include<math.h>
int main (){
    int n;
    printf("Enter a number which you want sqrt : ");
    scanf("%d",&n);
    int root = sqrt(n);
    printf("The sqrt root of %d is \n Answer = %d",n,root);
return 0;
}